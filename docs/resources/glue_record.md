---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "gandi_glue_record Resource - gandi"
subcategory: ""
description: |-
  Manages a glue record (host) for a Gandi domain, mapping a nameserver name to one or more IPs.
---

# gandi_glue_record (Resource)

Manages a glue record (host) for a Gandi domain, mapping a nameserver name to one or more IPs.

## Example Usage

```terraform
resource "gandi_glue_record" "ns1" {
  domain = "example.com"
  name   = "ns1"
  ips    = ["203.0.113.53"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `domain` (String) The domain (FQDN) the glue record belongs to.
- `ips` (Set of String) IPv4/IPv6 addresses for the host.
- `name` (String) Short host name, e.g. `ns1` for `ns1.example.com`.

### Read-Only

- `id` (String) `<domain>/<name>`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Glue records are imported as <domain>/<name>.
terraform import gandi_glue_record.ns1 example.com/ns1
```
